export const enableForeingKeys = `PRAGMA foreign_keys = ON`
export const beginTransaction = `BEGIN TRANSACTION`
export const commitTransaction = `COMMIT`
export const rollbackTransaction = `ROLLBACK`
//export const lastIdInsert = `SELECT LAST_INSERT_ID() AS id` // TODO: Check if it is necessary the "AS id" part

